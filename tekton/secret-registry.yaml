apiVersion: v1
kind: Secret
metadata:
  name: registry-credentials
  namespace: gitops-demo
  annotations:
    tekton.dev/docker-0: registry.airgap.local
stringData:
  username: mirror-user
  password: mirror-password
  email: mirror@example.com
